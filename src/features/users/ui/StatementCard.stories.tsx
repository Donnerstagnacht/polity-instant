import { USER } from '../state/user.data';
import type { Meta, StoryObj } from '@storybook/react-vite';
// import { StatementCard } from './StatementCard';

// Component not found, story disabled
const meta: Meta = {
  // component: StatementCard,
};

export default meta;

type Story = StoryObj;

/*
export const StatementCardDefault: Story = {
  render: args => (
    <StatementCard
      tag={USER.statements[0].tag}
      text={USER.statements[0].text}
      tagColor={{ bg: 'bg-blue-100', text: 'text-blue-700' }}
      {...args}
    />
  ),
};
*/
